@model KPI_Schedule.Models.Student

<h4>Ви впевнені, що хочете видалити?</h4>

<table>
    <tr>
        <th>Group</th>
        <th>Name</th>
        <th>Email</th>
        <th>Phone</th>
    </tr>
    <tr>
        <td>@Model.Group.Name</td>
        <td>@Model.Name</td>
        <td>@Model.Email</td>
        <td>@Model.Phone</td>
    </tr>
</table>
<form asp-action="Delete" asp-route-id="@Model.Id">
    <input type="submit" value="Delete" />
</form>
<a asp-action="List">Go Back</a>